import { AdminmoviesPipe } from './adminmovies.pipe';

describe('AdminmoviesPipe', () => {
  it('create an instance', () => {
    const pipe = new AdminmoviesPipe();
    expect(pipe).toBeTruthy();
  });
});
